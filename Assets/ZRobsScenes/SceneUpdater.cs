using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SceneUpdater : MonoBehaviour
{
    public int branch;
    public TextGenerator Textbox;

    // Start is called before the first frame update
    void Start()
    {
        branch = 1;
    }

    // Update is called once per frame
    void Update()
    {
        {

        }
    }

    void buttonPress()
    {

    }
}
